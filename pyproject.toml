[tool.poetry]
name = "auth-microservice"
version = "0.1.0"
description = ""
authors = ["m.malysh <m.malysh@mail.ocas.local>"]
readme = "README.md"
packages = [{include = "auth_microservice", from = "src"}]

[tool.poetry.dependencies]
python = "^3.12"
fastapi = "^0.115.5"
uvicorn = "^0.32.0"
sqlalchemy = "^2.0.36"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
